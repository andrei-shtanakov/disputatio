name: tdd-gate
description: "Audit-фоллбэк TDD-гейта: каждая DONE-задача с claim имеет verdict"
hooks:
  post_done:
    command: 'cd "$SR_PROJECT_ROOT" && uv run python scripts/tdd_gate.py audit'
    blocking: true
    run_on: on_success
